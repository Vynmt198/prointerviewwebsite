# `frontend/src/app/data`

Thư mục legacy chứa seed/mock FE đã **gỡ bỏ**. UI không dùng `coursesData` / `mockData` / `mentorMockData`; chỗ static nhẹ được đặt trong `src/app/constants/` (vd: `mentorFilterFields.js`, `reportCategories.js`).

Dữ liệu khách học việt chờ được Mongo/API trong các helper như `history.js` và các route trong ROADMAP.
