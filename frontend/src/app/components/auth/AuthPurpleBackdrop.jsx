import React from "react";

/** Cùng nền loang tím với Home (`AppLayout` → `app-shell-ambient--home`). */
export function AuthPurpleBackdrop() {
  return <div className="app-shell-ambient app-shell-ambient--home" aria-hidden />;
}
